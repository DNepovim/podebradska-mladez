extends layouts/default

block content
  main.som.col-md-10.col-sm-9

    - if ( have_posts() ):
    - while ( have_posts() ):
    - the_post();
        article.person
            .col-md-3
                figure.person-image.box(style="background-image: url('<?php the_post_thumbnail_url('som-face')?>')")
            .person-content.col-md-9
                h1.person-title=the_title()
                - the_content()
    - endwhile;
    - else:
      div='Nic zde není...'
    - endif